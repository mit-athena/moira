# $Id: Makefile.in,v 1.3 1998-08-27 17:13:27 danw Exp $

SHELL=/bin/sh
@SET_MAKE@

CC=@CC@
CPPFLAGS=@CPPFLAGS@
CFLAGS=@CFLAGS@
DEFS=@DEFS@
ALL_CFLAGS=$(CPPFLAGS) $(CFLAGS) $(DEFS)
LDFLAGS=@LDFLAGS@
LIBS=@LIBS@
MR_LIBDEP=@MR_LIBDEP@
INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@

srcdir=@srcdir@
VPATH=@srcdir@
SRCTOP=@top_srcdir@
BUILDTOP=../..
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@

OBJS=chfn.o chpobox.o chsh.o

TARGET=chfn chpobox chsh

.c.o:
	$(CC) -c $(ALL_CFLAGS) $<

all: $(TARGET)

clean:
	rm -f $(OBJS) $(TARGET)

depend:

install: all
	$(INSTALL_PROGRAM) chfn $(bindir)
	$(INSTALL_PROGRAM0 chpobox $(bindir)
	$(INSTALL_PROGRAM) chsh $(bindir)

chfn: chfn.o $(MR_LIBDEP)
	$(CC) -o $@ $(LDFLAGS) $@.o $(LIBS)

chpobox: chpobox.o $(MR_LIBDEP)
	$(CC) -o $@ $(LDFLAGS) $@.o $(LIBS)

chsh: chsh.o $(MR_LIBDEP)
	$(CC) -o $@ $(LDFLAGS) $@.o $(LIBS)
