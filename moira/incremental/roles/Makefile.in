# $Id$

SHELL=/bin/sh
@SET_MAKE@

INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@

srcdir=@srcdir@
VPATH=@srcdir@
SRCTOP=@top_srcdir@
BUILDTOP=../..
mrbindir=@mrbindir@

TARGET=roles.incr

all:

clean:

cleandir distclean:

depend:

install:
	${INSTALL_PROGRAM} ${TARGET} $(DESTDIR)${mrbindir}
